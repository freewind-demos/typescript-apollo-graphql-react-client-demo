TypeScript Apollo Graphql React Client Demo
===========================================

Apollo除了有graphql server，也有client，提供了react hooks帮助向server发请求取数据。

```
npm install
npm run demo
```

It will open page on browser automatically.
